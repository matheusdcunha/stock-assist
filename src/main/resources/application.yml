spring:
  application:
    name: 'stock-assist'
  data:
    mongodb:
      auto-index-creation: true
      authentication-database: 'admin'
      host: 'localhost'
      port: 27017
      database: 'stockassistdb'
      username: 'admin'
      password: '123'
app:
  client-id: '${APP_CLIENT_ID : ABC}'
  client-secret: '${APP_CLIENT_SECRET: DEF}'
